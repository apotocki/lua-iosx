name: Build LUA
on:
  release:
    types: [published]
  push:
    tags:
        - 5.4.4.**
  workflow_dispatch:
jobs:
  Build:
    runs-on: macos-latest
    timeout-minutes: 120
    steps:
    - uses: actions/checkout@v3
    - name: Build
      run: |
        scripts/build.sh
        for i in frameworks/*/; do cd frameworks && zip -9 -r "$(basename -- $i).zip" $(basename -- $i) & done; wait
        mv frameworks/Headers frameworks/include
        cd frameworks && zip -9 include.zip include
        wait
    - name: Release
      uses: softprops/action-gh-release@v1
      if: startsWith(github.ref, 'refs/tags/')
      with:
        files: |
          frameworks/*.zip
